package com.mycompany.iaeval.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class EvaluationAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEvaluationAllPropertiesEquals(Evaluation expected, Evaluation actual) {
        assertEvaluationAutoGeneratedPropertiesEquals(expected, actual);
        assertEvaluationAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEvaluationAllUpdatablePropertiesEquals(Evaluation expected, Evaluation actual) {
        assertEvaluationUpdatableFieldsEquals(expected, actual);
        assertEvaluationUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEvaluationAutoGeneratedPropertiesEquals(Evaluation expected, Evaluation actual) {
        assertThat(actual)
            .as("Verify Evaluation auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEvaluationUpdatableFieldsEquals(Evaluation expected, Evaluation actual) {
        assertThat(actual)
            .as("Verify Evaluation relevant properties")
            .satisfies(a -> assertThat(a.getScoreGlobal()).as("check scoreGlobal").isEqualTo(expected.getScoreGlobal()))
            .satisfies(a -> assertThat(a.getScoreAdmin()).as("check scoreAdmin").isEqualTo(expected.getScoreAdmin()))
            .satisfies(a -> assertThat(a.getScoreTech()).as("check scoreTech").isEqualTo(expected.getScoreTech()))
            .satisfies(a -> assertThat(a.getScoreFin()).as("check scoreFin").isEqualTo(expected.getScoreFin()))
            .satisfies(a -> assertThat(a.getRapportAnalyse()).as("check rapportAnalyse").isEqualTo(expected.getRapportAnalyse()))
            .satisfies(a -> assertThat(a.getDocumentPv()).as("check documentPv").isEqualTo(expected.getDocumentPv()))
            .satisfies(a ->
                assertThat(a.getDocumentPvContentType()).as("check documentPv contenty type").isEqualTo(expected.getDocumentPvContentType())
            )
            .satisfies(a -> assertThat(a.getDateEvaluation()).as("check dateEvaluation").isEqualTo(expected.getDateEvaluation()))
            .satisfies(a -> assertThat(a.getEstValidee()).as("check estValidee").isEqualTo(expected.getEstValidee()))
            .satisfies(a ->
                assertThat(a.getCommentaireEvaluateur()).as("check commentaireEvaluateur").isEqualTo(expected.getCommentaireEvaluateur())
            );
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEvaluationUpdatableRelationshipsEquals(Evaluation expected, Evaluation actual) {
        // empty method
    }
}
