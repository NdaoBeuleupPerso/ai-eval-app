package com.mycompany.iaeval.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CritereAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCritereAllPropertiesEquals(Critere expected, Critere actual) {
        assertCritereAutoGeneratedPropertiesEquals(expected, actual);
        assertCritereAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCritereAllUpdatablePropertiesEquals(Critere expected, Critere actual) {
        assertCritereUpdatableFieldsEquals(expected, actual);
        assertCritereUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCritereAutoGeneratedPropertiesEquals(Critere expected, Critere actual) {
        assertThat(actual)
            .as("Verify Critere auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCritereUpdatableFieldsEquals(Critere expected, Critere actual) {
        assertThat(actual)
            .as("Verify Critere relevant properties")
            .satisfies(a -> assertThat(a.getNom()).as("check nom").isEqualTo(expected.getNom()))
            .satisfies(a -> assertThat(a.getPonderation()).as("check ponderation").isEqualTo(expected.getPonderation()))
            .satisfies(a -> assertThat(a.getCategorie()).as("check categorie").isEqualTo(expected.getCategorie()))
            .satisfies(a -> assertThat(a.getDescription()).as("check description").isEqualTo(expected.getDescription()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCritereUpdatableRelationshipsEquals(Critere expected, Critere actual) {
        assertThat(actual)
            .as("Verify Critere relationships")
            .satisfies(a -> assertThat(a.getAppelOffre()).as("check appelOffre").isEqualTo(expected.getAppelOffre()));
    }
}
