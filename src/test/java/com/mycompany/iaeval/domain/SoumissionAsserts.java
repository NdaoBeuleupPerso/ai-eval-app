package com.mycompany.iaeval.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class SoumissionAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSoumissionAllPropertiesEquals(Soumission expected, Soumission actual) {
        assertSoumissionAutoGeneratedPropertiesEquals(expected, actual);
        assertSoumissionAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSoumissionAllUpdatablePropertiesEquals(Soumission expected, Soumission actual) {
        assertSoumissionUpdatableFieldsEquals(expected, actual);
        assertSoumissionUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSoumissionAutoGeneratedPropertiesEquals(Soumission expected, Soumission actual) {
        assertThat(actual)
            .as("Verify Soumission auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSoumissionUpdatableFieldsEquals(Soumission expected, Soumission actual) {
        assertThat(actual)
            .as("Verify Soumission relevant properties")
            .satisfies(a -> assertThat(a.getDateSoumission()).as("check dateSoumission").isEqualTo(expected.getDateSoumission()))
            .satisfies(a -> assertThat(a.getStatut()).as("check statut").isEqualTo(expected.getStatut()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSoumissionUpdatableRelationshipsEquals(Soumission expected, Soumission actual) {
        assertThat(actual)
            .as("Verify Soumission relationships")
            .satisfies(a -> assertThat(a.getEvaluation()).as("check evaluation").isEqualTo(expected.getEvaluation()))
            .satisfies(a -> assertThat(a.getAppelOffre()).as("check appelOffre").isEqualTo(expected.getAppelOffre()))
            .satisfies(a -> assertThat(a.getCandidat()).as("check candidat").isEqualTo(expected.getCandidat()));
    }
}
