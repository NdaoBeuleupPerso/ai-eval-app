package com.mycompany.iaeval.domain.enumeration;

public enum StatutCritere {
    A_VALIDER, // Suggéré par l'IA
    VALIDE, // Validé par l'Admin
    REJETE, // Rejeté par l'Admin (ne sera pas utilisé pour l'évaluation)
}
